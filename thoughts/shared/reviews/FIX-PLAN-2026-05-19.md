# Fix Plan — Adversary Reviews (2026-05-18)

Status: draft. Source docs in `thoughts/shared/reviews/*-2026-05-18.md`.

8 reviewers found 18 release-blockers + ~30 high. 4-wave plan below.

## Wave 1 — release blockers (1 day, all <30 LOC)

| # | Issue | File | Fix |
|---|---|---|---|
| 1.1 | Voice mod guard dead | `voice_assistant_overlay.gd:111` | `last_result.get("blocked")` → `not last_result.get("allowed", true)` |
| 1.2 | COPPA delete leaves consent | `filesystem_consent_store.gd` + `filesystem_data_lifecycle_adapter.gd:160-174` | Add `delete_profile()` API, remove `_save()` duck-type |
| 1.3 | COPPA auth default-allow | `manage_data_lifecycle_service.gd:166-213` | Empty/non-Array `managed_profiles` → deny; audit denials; populate in `_build_default_profile` |
| 1.4 | Safety-block silent | `voice_input_moderation_service.gd` + `voice_assistant_overlay.gd` | Play `block_buzz` + `block_oops`; regen `block_buzz` at -3dBFS |
| 1.5 | Nunito-Bold fake | `data/fonts/Nunito-Bold.ttf` | Replace with real Google Fonts bold |
| 1.6 | Theme silently broken | `main.gd:801-804` + `.gitignore` | `push_error` on null; commit `.import` sidecars |
| 1.7 | Title hierarchy inverted | `create/library/parent/play.tscn` line 33-40 | Remove `font_size=26` override (theme Label=28) |
| 1.8 | Template→palette never switches | `create_shell.gd:738-744,808-812` | Look up palette via `palettes.json#template_defaults[id]` |
| 1.9 | Landing Polish skips _t() | `landing_screen.gd:142,157,163,174` + `polish_localization_policy.gd` | Route through `_localization.translate()`, add `landing.*` keys |
| 1.10 | 1920x1080 coords clip viewport | `landing_screen.gd:80-83,113,209,342` | Replace absolute `Vector2` with anchor presets |
| 1.11 | `_apply_active_tool` bypasses gate | `create_shell.gd:375` | First line: `if not _ports_ready: return` |

Source: safety C1/C2/C3/C4, audio C1/C2, visual Critical x3, ux C1/C2/C3, godot-arch C1/C2.

## Wave 2 — high before beta (3-7 days)

| # | Issue | Files | Notes |
|---|---|---|---|
| 2.1 | Game rules engine MVP | new `rules_engine_service.gd`, `quest_progress_service.gd`; `gameplay_runtime.gd`; `play_shell.gd` | TIMER+SCORING only in v1. Currently celebration shows 0/0/0. |
| 2.2 | Blender asset triage | `world_renderer.gd:37-74`, prop glTFs, `mascot.gd` | DECISION: trim map to 7 OR regen 23 props. Plus `fence_segment.gltf`→`fence.gltf` 1-line fix, real `baseColorFactor`, wire `bunny.gltf` |
| 2.3 | Audio loops + music stop | `generate_eleven_assets.py`, mp3s, `play_shell.gd:258`, `audio_bank.gd` | ffmpeg silenceremove on all mp3s. `stop_music` on session start. `spawn_pop`/`victory_fanfare` to -3dBFS |
| 2.4 | Godot arch fixes | `main.tscn`, `main.gd:169-189`, `onboarding_service.gd:85` | Shell visibility defaults, EnvironmentPort plumbing, event_bus DI |
| 2.5 | UX polish | `create_shell.gd:1078`, `mascot.gd:72-85`, `button_feel.gd:55`, `audio_bank.gd`, `voice_assistant_overlay.gd:36` | Persistent Timer node, speech panel bg, deferred pivot, 250ms hover throttle, 88px record button |
| 2.5b | Wire orphan audio (decision 2026-05-19) | `sfx_player.gd:30`, `landing_screen.gd:278`, `play_shell.gd:340`, `create_shell.gd:_apply_active_tool`, `gameplay_runtime.gd:54`, `audio_bank.gd` | `celebrate_collect` voice on every pickup; `no_world` voice on empty-picker; `session_end` voice on celebration close; `tools_unavailable` voice on ports_ready=false toast; `celebration` music on victory; `spawn_pop` SFX on player spawn; swap `ui_click`/`ui_hover` from 1s ElevenLabs to ~100ms Kenney equivalents (audio review H2/H3) |
| 2.5c | Larger audio wires (carry-over) | `gameplay_runtime.gd`, `player_controller.gd:99` | `collect_star` SFX needs trigger-type metadata in Area3D; `footstep_sand` SFX needs surface-type emit on player controller — both deferred to W3.5 game-content wave |
| 2.6 | Disabled button style | `choyce_theme.tres:319` | New `StyleBoxFlat_disabled` grey, 4.5:1 contrast |
| 2.7 | Parent zone dead buttons | `parent_zone_shell.gd:121-130` | Connect undo + safe-restore, or hide |
| 2.8 | _t() for mascot + voice card | `mascot.gd:166`, `voice_assistant_card.gd:13,49` | Fix typos (Wiecej→Więcej, Slyszę→Słyszę), route through _t() |

## Wave 3 — pre-beta polish (1-2 weeks)

- A11y persistence (safety H4)
- COPPA delete friction — typed "USUŃ" or countdown (safety H3)
- Parent zone ports_ready gate (safety H2)
- Audio bus layout — Master/Music/Voice/SFX + compressor (audio H4)
- Game content depth: TemplateLoader→starter seeding, fall→respawn, undo wiring, PAINT key fix, empty-picker guard (game-design G-004/005/007/009/010/014)
- Landing texture optimization: sky 1920x1080, clouds 512x256, target <600KB (was ~7MB)
- Repo hygiene: ~18MB cleanup (Kenney FBX/OBJ, particle PNG black-bg, THIRD_PARTY_LICENSES.md). **Kenney audio kept** — adopted as canonical for short UI cues in W2.5b (replaces 1s ElevenLabs ui_click/ui_hover with tight ~100ms Kenney clips).
- Mascot palette-awareness (visual L)

## Wave 4 — deferred / future portability (post-beta)

- Unity port abstractions: CryptoPort, ClockPort, IdGeneratorPort, TemplateCatalogPort, LoggerPort (unity-port full review)
- WorldRenderer perf: ShaderMaterial caching, threaded glTF load (godot-arch M3/M4)
- AudioBank LRU caps (godot-arch M5, audio L7)
- WorldRenderer.clear_world sync removal (godot-arch M6)
- ButtonFeel scope to UI subtree (godot-arch M1)
- AuditRecord._extract_payload per-event-type (unity-port M-06)
- Vault keyfile hardening (safety M1)

## Open questions for product

1. **Blender (W2.2)**: Track A trim to 7 props (fast) OR Track B regen 23 missing (1-2 days, matches design)?
2. **Rules engine scope (W2.1)**: v1 TIMER+SCORING OR + WIN_CONDITION + EVENT_TRIGGER (2x effort, enables obby wins)?
3. **Wave 1 packaging**: single PR with all 11 fixes OR one PR per fix?

## Verification

```bash
timeout 30 godot --editor --headless --quit 2>&1 | grep -E "SCRIPT ERROR|Parse"  # empty
godot --headless --script tests/run_all_tests.gd  # 0 failures
rm -rf ~/Library/Application\ Support/choyce_engine/projects/local_kid_1_*
godot --path .
# Smoke: theme styled, Polish via _t(), no shell-stack flash, gameplay records stats,
# celebration shows real numbers, parent Export/Delete gated, undo works.
```

## Cross-tally — issues logged by 2+ reviewers

- Landing Polish bypasses _t(): ux C1, visual L1/L2, godot-arch M7
- 1920x1080 absolute coords clip viewport: ux C2, visual multi
- Disabled Button looks like secondary CTA: ux L6, visual Critical
- Hardcoded colors bypass palettes.json: ux, visual C3
- Hover SFX flood, pool exhausts: ux H5/L5, audio H2/H3
- ButtonFeel global node_added tax: godot-arch M1/M2, visual
- Theme silently fails: godot-arch C1/C2, visual Critical
- managed_profiles default-allow: safety C3/M2
- Quest tracker / stats never written: game-design G-003/G-006
- .hash() for non-crypto IDs: unity-port M-01 (recurring)
